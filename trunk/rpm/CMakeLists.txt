PROJECT (XTL_RPM)

CMAKE_MINIMUM_REQUIRED(VERSION 2.4)

SET (XTL_PACKAGE_NAME "sup-xtl")

CONFIGURE_FILE (
	xtl.spec.in
	${CMAKE_CURRENT_BINARY_DIR}/xtl.spec
	@ONLY
)

CONFIGURE_FILE(
	build-sup-xtl.sh.in
	${CMAKE_CURRENT_BINARY_DIR}/build-sup-xtl.sh
	@ONLY
)
